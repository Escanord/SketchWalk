# Runtime dependencies for SketchWalk inference on LongBench.
#
# Install a PyTorch build matching your CUDA version separately, e.g.
#   pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/cu128
# then install everything below:
#   pip install -r requirements.txt

# --- Core: model + inference ---
transformers==4.57.0
tokenizers==0.22.1
accelerate==1.10.1
safetensors==0.6.2
huggingface-hub==0.35.3
hf-xet==1.1.10
triton==3.4.0

# --- Data loading ---
datasets==3.1.0
pyarrow==21.0.0
numpy==2.3.3

# --- LongBench eval metrics ---
rouge==1.0.1
fuzzywuzzy==0.18.0
jieba==0.42.1

# --- Utility ---
tqdm==4.67.1
